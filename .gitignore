# ---------------------------------------------------------------------------
# Corpus files — full article text is copyrighted and kept local only.
# Only metadata + analysis results are committed/deployed.
# ---------------------------------------------------------------------------
# Corpus TSV files (full article text — copyrighted, local only)
*.tsv
# Source spreadsheets (contain bios/descriptions, kept local)
*.xlsx
*.xls
# Pre-computed public results (generated locally, deployed separately)
streamlit_app/data/results_public.csv
# data/ files that ARE committed (metadata only, no article text):
#   streamlit_app/data/expressions.json
#   streamlit_app/data/authors.csv
#   streamlit_app/data/name_mapping.csv
#   streamlit_app/data/name_candidates.csv
#   streamlit_app/data/precomputed_results.csv   ← generated by scripts/precompute.py
#   streamlit_app/data/corpus_meta.csv           ← generated by scripts/precompute.py

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# ---------------------------------------------------------------------------
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
